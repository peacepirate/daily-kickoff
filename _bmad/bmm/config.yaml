# BMad project config — Daily Kickoff Multi-Topic Digest Pipeline
# {project-root} tokens are resolved at runtime by the consuming LLM

project_name: "Daily Kickoff — Multi-Topic Digest Pipeline"
output_folder: "{project-root}/_bmad-output"
document_output_language: "English"
planning_artifacts: "{project-root}/.claude/planning"
implementation_artifacts: "{project-root}/.claude/stories"
project_context: "{project-root}/.claude/planning/project-context.md"
project_knowledge: "{project-root}/.claude/planning"
